// 1. How to declare a variable using let and const
const fathername = 'Alomgir';
let season = 'summer';
season = 'rainy';

// 6 basic condition : >, <, <=, >=, ===, !==
// multiple conditions && or || ......

if (fathername === 'alomgir' && season === 'rainy') {
    
}
else if (fathername === 'Alomgir') {
    
}
else {
    
};

// 3. array
// index, set
// length, push
const numbers = [89, 35, 98, 12];
numbers[1] = 56;

// 4. for loop
// for (let i = 0; i < numbers.length; i++) {
//     const number = numbers[i];
//     console.log(number);
// }
for (number of numbers) {
    console.log(number);
};

// 5. function
const multiply = (num1, num2) => { return num1 * num2 };


